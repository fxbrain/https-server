{
  "name": "https_server",
  "version": "1.0.0",
  "description": "https TLS server",
  "main": "https-server.js",
  "scripts": {
    "test": "node built/test/test.js",
    "build": "tsd install && tsc && exit 0;",
    "defs": "tsd install"
  },
  "author": "Stefan Schwarz",
  "license": "MIT",
  "dependencies": {
    "http2": "^3.2.0"
  }
}
